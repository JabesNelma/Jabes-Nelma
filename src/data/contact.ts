import content from './content';

// Export contact links dari content.ts
export const contactLinks = content.contactLinks || [];

// Export current focus dari content.ts
export const currentFocus = content.currentFocus || [];

export default { contactLinks, currentFocus };
